-- Новый вид абонемента: 9 месяцев (учебный год).
-- Отдельный файл: добавленное значение enum нельзя использовать в той же
-- транзакции, где оно создано, — а следующая миграция (card_plans) на него
-- уже ссылается.

alter type card_type add value if not exists 'nine_month';
